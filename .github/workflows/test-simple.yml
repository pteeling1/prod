name: Generate Blog from Azure Docs Changes

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * 1'

jobs:
  generate-blog:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      
      - name: Test workflow
        run: echo "✅ Workflow triggered successfully"
